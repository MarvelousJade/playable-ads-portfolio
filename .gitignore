# dependencies (Playwright is large — only needed for headless testing)
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
.vscode/

# personal résumé working notes — kept out of the public site
DOSSIER.md

# personal resume — kept out of the public site
resume-playable-ads.tex
